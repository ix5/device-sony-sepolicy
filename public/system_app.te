# System-specifc policy for coredomain system_app

# SELinux status
r_dir_file(system_app, selinuxfs)

# For android setttings
binder_call(system_app, wificond)
binder_call(system_app, update_engine)
