get_prop(vold, tee_listener_prop)

# TODO: Is this necessary?
# Cannot reproduce on tone
# perhaps exfat kernel module? for omni?
# -> nope, it was encryption on omni
# avc: denied { module_request } for kmod="crypto-cryptd(__driver-cbc-aes-ce)" scontext=u:r:vold:s0 tcontext=u:r:kernel:s0 tclass=system permissive=1
allow vold kernel:system module_request;
userdebug_or_eng(`
  auditallow vold kernel:system module_request;
')

allow vold persist_file:dir r_dir_perms;

allow vold proc_swaps:file r_file_perms;
allow vold {
      system_block_device
      adsprpcd_block_device
      persist_block_device
}:blk_file getattr;

# generated by proc scan, unnecessary
dontaudit vold proc_irq:dir { read open };
